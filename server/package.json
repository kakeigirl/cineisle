{
  "name": "cineisle-server",
  "version": "0.4.7",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node -e \"require('express'); require('cors'); console.log('CineIsle dependencies OK')\"",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": "20.x"
  }
}
